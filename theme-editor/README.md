## Theme Editor

PySide6 port of the Qlementine C++ theme editor utility.

### Run

```bash
uv run main.py
```

The editor loads built-in presets from ../themes, applies changes live through PySide6-Qlementine, and can load or save full theme JSON files from disk.
